@extends('layouts.app', ['title' => 'قوالب الشهادات'])
@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="mb-2 text-sm font-bold text-gold">إدارة التصميمات</p><h1 class="text-3xl font-bold text-emerald-950">قوالب الشهادات</h1><p class="mt-2 text-slate-500">أنشئ قوالب شهادات قابلة لإصدارها للطلاب.</p></div><a href="{{ route('certificate-templates.create') }}" class="inline-flex items-center justify-center gap-2 rounded-2xl bg-emerald-950 px-5 py-3.5 font-bold text-white"><i class="bx bx-plus text-xl text-gold"></i> قالب جديد</a></div>
<div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">@forelse($templates as $template)<div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><img src="{{ Storage::disk('public')->url($template->background_path) }}" alt="{{ $template->name }}" class="aspect-[4/3] w-full object-cover"><div class="p-5"><div class="flex items-start justify-between gap-3"><div><h2 class="font-bold text-emerald-950">{{ $template->name }}</h2><p class="mt-1 text-sm text-slate-400">{{ $template->width }} × {{ $template->height }} بكسل</p></div><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">نشط</span></div><div class="mt-5 flex gap-2"><a href="{{ route('certificate-templates.edit', $template) }}" class="flex-1 rounded-xl bg-amber-50 py-2 text-center text-sm font-bold text-amber-700">تعديل</a><form method="POST" action="{{ route('certificate-templates.destroy', $template) }}" data-confirm="هل تريد حذف هذا القالب؟" class="flex-1">@csrf @method('DELETE')<button class="w-full rounded-xl bg-red-50 py-2 text-sm font-bold text-red-600">حذف</button></form></div></div></div>@empty<div class="col-span-full rounded-3xl border border-dashed border-slate-300 bg-white p-14 text-center text-slate-400">لا توجد قوالب بعد.</div>@endforelse</div>
@endsection
