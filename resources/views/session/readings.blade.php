@extends('layouts.app', ['title' => 'اختيار القراءة'])
@section('content')
<div class="mb-8"><a href="{{ route('session.students') }}" class="mb-4 inline-flex items-center gap-1 text-sm font-bold text-slate-500"><i class="bx bx-right-arrow-alt"></i> تغيير الطالب</a><p class="mb-2 text-sm font-bold text-gold">الخطوة الثانية</p><h1 class="text-3xl font-bold text-emerald-950">اختر القراءة</h1><p class="mt-2 text-slate-500">الطالب المختار: <strong class="text-emerald-900">{{ $student->name }}</strong></p></div>
<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($qiraats as $qiraat)@php($isComplete = $qiraat->narrations->count() === 2 && $qiraat->narrations->every(fn ($narration) => $narration->progress->first()?->is_complete))<a href="{{ route('session.narrations', [$student, $qiraat]) }}" class="group rounded-3xl border p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg {{ $isComplete ? 'border-gold bg-gradient-to-br from-emerald-950 to-emerald-800 text-white shadow-emerald-950/20' : 'border-slate-200 bg-white hover:border-gold' }}"><div class="mb-7 flex items-start justify-between"><div class="grid h-12 w-12 place-items-center rounded-2xl text-2xl {{ $isComplete ? 'bg-gold text-emerald-950' : 'bg-emerald-100 text-emerald-800' }}"><i class="{{ $isComplete ? 'bx bx-check-circle' : 'bx bx-book-reader' }}"></i></div><i class="bx bx-left-arrow-alt text-2xl text-gold transition group-hover:-translate-x-1"></i></div><h2 class="text-xl font-bold {{ $isComplete ? 'text-white' : 'text-emerald-950' }}">{{ $qiraat->name }}</h2><p class="mt-1 text-sm {{ $isComplete ? 'text-emerald-100/70' : 'text-slate-400' }}">الإمام: {{ $qiraat->imam }}</p><div class="mt-6 border-t pt-4 text-sm {{ $isComplete ? 'border-white/15' : 'border-slate-100' }}">@if($isComplete)<span class="inline-flex items-center gap-2 font-bold text-gold"><i class="bx bx-check-circle text-lg"></i> أتم الروايتين</span>@else<span class="font-bold text-emerald-800">روايتان متاحتان للتسميع</span>@endif</div></a>@endforeach</div>
@endsection
