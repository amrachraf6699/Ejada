@extends('layouts.app', ['title' => 'الشهادات الصادرة'])
@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="mb-2 text-sm font-bold text-gold">سجل التكريم</p><h1 class="text-3xl font-bold text-emerald-950">الشهادات الصادرة</h1><p class="mt-2 text-slate-500">يمكنك إعادة تنزيل الشهادات أو مراجعة سجل إصدارها.</p></div><a href="{{ route('certificates.create') }}" class="inline-flex items-center justify-center gap-2 rounded-2xl bg-emerald-950 px-5 py-3.5 font-bold text-white"><i class="bx bx-plus text-xl text-gold"></i> إصدار شهادة</a></div>
<div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">@forelse($certificates as $certificate)<div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><img src="{{ Storage::disk('public')->url($certificate->image_path) }}" alt="شهادة {{ $certificate->student_name_snapshot }}" class="aspect-[4/3] w-full object-cover"><div class="p-5"><h2 class="font-bold text-emerald-950">{{ $certificate->student_name_snapshot }}</h2><p class="mt-1 text-sm text-slate-400">{{ $certificate->template?->name ?? 'قالب محذوف' }} · {{ $certificate->issued_at?->locale('ar')->translatedFormat('j F Y') }}</p><div class="mt-5 flex gap-2"><a href="{{ route('certificates.download', $certificate) }}" class="flex-1 rounded-xl bg-emerald-50 py-2 text-center text-sm font-bold text-emerald-700">تنزيل</a><form method="POST" action="{{ route('certificates.destroy', $certificate) }}" data-confirm="هل تريد حذف هذه الشهادة؟" class="flex-1">@csrf @method('DELETE')<button class="w-full rounded-xl bg-red-50 py-2 text-sm font-bold text-red-600">حذف</button></form></div></div></div>@empty<div class="col-span-full rounded-3xl border border-dashed border-slate-300 bg-white p-14 text-center text-slate-400">لا توجد شهادات صادرة بعد.</div>@endforelse</div>@if($certificates->hasPages())<div class="mt-6">{{ $certificates->links() }}</div>@endif
@endsection
