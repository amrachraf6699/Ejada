@extends('layouts.app', ['title' => 'إحصائيات الطالب'])
@section('content')
<div class="mb-8"><a href="{{ route('analytics') }}" class="mb-4 inline-flex items-center gap-1 text-sm font-bold text-slate-500"><i class="bx bx-right-arrow-alt"></i> الإحصائيات</a><h1 class="text-3xl font-bold text-emerald-950">إحصائيات {{ $student->name }}</h1><p class="mt-2 text-slate-500">ملخص تقدم الطالب حسب كل قراءة.</p></div><div class="mb-8 grid gap-5 sm:grid-cols-2"><div class="rounded-3xl bg-emerald-950 p-6 text-white"><i class="bx bx-check-circle text-3xl text-gold"></i><div class="mt-5 text-3xl font-bold">{{ $ayahCount }}</div><p class="mt-1 text-emerald-100/60">آيات مؤكدة</p></div><div class="rounded-3xl border border-slate-200 bg-white p-6"><i class="bx bx-play-circle text-3xl text-emerald-700"></i><div class="mt-5 text-3xl font-bold text-emerald-950">{{ $sessions }}</div><p class="mt-1 text-slate-400">جلسات التسميع</p></div></div><section class="rounded-3xl border border-slate-200 bg-white p-5 sm:p-7"><h2 class="mb-5 text-xl font-bold text-emerald-950">التقدم حسب القراءة</h2><div class="grid gap-4 sm:grid-cols-2">@forelse($student->progress as $progress)<div class="rounded-2xl bg-sand p-5"><div class="flex justify-between gap-3"><span class="font-bold text-emerald-950">{{ $progress->qiraat->name }}</span><span class="text-sm text-emerald-700">{{ $progress->global_ayah_number }} آية</span></div><p class="mt-3 text-sm text-slate-500">آخر موضع: {{ $progress->surah_name }}، الآية {{ $progress->ayah_number }}</p></div>@empty<div class="py-8 text-slate-400">لم يبدأ الطالب التسميع بعد.</div>@endforelse</div></section><section class="mt-8 rounded-3xl border border-slate-200 bg-white p-5 sm:p-7"><h2 class="mb-5 text-xl font-bold text-emerald-950">آخر النشاطات</h2><div class="space-y-3">@forelse($activity as $item)<div class="flex items-center justify-between border-b border-slate-100 py-3 last:border-0"><span class="font-bold">{{ $item->surah_name }}، الآية {{ $item->ayah_number }}</span><span class="text-sm text-slate-400">{{ $item->confirmed_at?->locale('ar')->translatedFormat('j F، H:i') }}</span></div>@empty<div class="py-8 text-slate-400">لا يوجد نشاط بعد.</div>@endforelse</div></section>
@endsection
