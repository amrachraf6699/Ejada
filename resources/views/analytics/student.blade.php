@extends('layouts.app', ['title' => 'إحصائيات الطالب'])
@section('content')
<div class="mb-8"><a href="{{ route('analytics') }}" class="mb-4 inline-flex items-center gap-1 text-sm font-bold text-slate-500"><i class="bx bx-right-arrow-alt"></i> الإحصائيات</a><h1 class="text-3xl font-bold text-emerald-950">إحصائيات {{ $student->name }}</h1><p class="mt-2 text-slate-500">تفاصيل شاملة لتقدم الطالب ونشاطه.</p></div>
<div class="mb-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><div class="rounded-3xl bg-emerald-950 p-6 text-white"><i class="bx bx-check-circle text-3xl text-gold"></i><div class="mt-5 text-3xl font-bold">{{ $savedAyahsAcrossReadings }}</div><p class="mt-1 text-emerald-100/60">مجموع التقدم عبر القراءات</p></div><div class="rounded-3xl border border-slate-200 bg-white p-6"><i class="bx bx-book-open text-3xl text-emerald-700"></i><div class="mt-5 text-3xl font-bold text-emerald-950">{{ $ayahCount }}</div><p class="mt-1 text-slate-400">تأكيدات التسميع</p></div><div class="rounded-3xl border border-slate-200 bg-white p-6"><i class="bx bx-play-circle text-3xl text-emerald-700"></i><div class="mt-5 text-3xl font-bold text-emerald-950">{{ $sessions }}</div><p class="mt-1 text-slate-400">جلسات التسميع</p></div><div class="rounded-3xl border border-slate-200 bg-white p-6"><i class="bx bx-calendar-check text-3xl text-gold"></i><div class="mt-5 text-lg font-bold text-emerald-950">{{ $lastActivity?->confirmed_at?->locale('ar')->translatedFormat('j F Y، H:i') ?? 'لا يوجد نشاط' }}</div><p class="mt-1 text-slate-400">آخر نشاط</p></div></div>
<section class="rounded-3xl border border-slate-200 bg-white p-5 sm:p-7"><div class="mb-6"><h2 class="text-xl font-bold text-emerald-950">التقدم حسب القراءة</h2><p class="mt-1 text-sm text-slate-400">كل قراءة مستقلة، والنسبة محسوبة من أصل 6236 آية.</p></div><div class="grid grid-cols-2 gap-5 sm:grid-cols-3 lg:grid-cols-5">@foreach($readingProgress as $reading)<div class="rounded-2xl bg-sand p-4 text-center"><div class="mx-auto grid h-28 w-28 place-items-center rounded-full" style="background: conic-gradient(#c99b4a {{ $reading->percentage }}%, #e8e8e8 0)"><div class="grid h-24 w-24 place-items-center rounded-full bg-white"><div><div class="text-lg font-bold text-emerald-950">{{ $reading->percentage }}%</div><div class="text-[11px] text-slate-400">{{ $reading->saved_ayahs }} / 6236</div></div></div></div><h3 class="mt-4 font-bold text-emerald-950">{{ $reading->name }}</h3><p class="mt-1 text-xs text-slate-500">{{ $reading->imam }}</p>@if($reading->progress)<p class="mt-3 text-xs text-emerald-800">آخر موضع: {{ $reading->progress->surah_name }}، الآية {{ $reading->progress->ayah_number }}</p>@else<p class="mt-3 text-xs text-slate-400">لم يبدأ التسميع بعد</p>@endif</div>@endforeach</div></section>
<section class="mt-8 rounded-3xl border border-slate-200 bg-white p-5 sm:p-7"><h2 class="mb-5 text-xl font-bold text-emerald-950">آخر جلسات التسميع</h2><div class="space-y-3">@forelse($activity as $item)<div class="border-b border-slate-100 py-3 last:border-0"><div class="flex flex-wrap items-center justify-between gap-2"><span class="font-bold">من {{ $item->start->surah_name }}، الآية {{ $item->start->ayah_number }} إلى {{ $item->end->surah_name }}، الآية {{ $item->end->ayah_number }}</span><span class="text-sm text-slate-400">{{ $item->confirmed_at?->locale('ar')->translatedFormat('j F، H:i') }}</span></div><div class="mt-1 text-sm text-slate-500">{{ $item->qiraat_name }} · {{ $item->ayah_count }} آية مؤكدة في جلسة واحدة</div></div>@empty<div class="py-8 text-slate-400">لا يوجد نشاط بعد.</div>@endforelse</div></section>
@endsection
